pub async fn run(ctx, input) {
    let agent = ctx.createAgent(#{
        model: input.model,
    }).await;

    agent.run([
        ctx.input.text("Answer concisely."),
        ctx.input.text(input.prompt),
    ], #{}).await
}

pub fn to_tui_markdown(result) {
    result.finalResponse
}
