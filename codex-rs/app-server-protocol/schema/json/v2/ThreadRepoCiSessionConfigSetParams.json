{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "ImplementMode": {
      "enum": [
        "auto",
        "implicit"
      ],
      "type": "string"
    },
    "RepoCiIssueType": {
      "enum": [
        "correctness",
        "reliability",
        "performance",
        "scalability",
        "security",
        "maintainability",
        "testability",
        "observability",
        "compatibility",
        "uxConfigCli"
      ],
      "type": "string"
    },
    "RepoCiSessionMode": {
      "enum": [
        "off",
        "local",
        "remote",
        "localAndRemote"
      ],
      "type": "string"
    }
  },
  "properties": {
    "implementEnabled": {
      "description": "Omit to leave unchanged; null clears the session override and returns to repo/user config.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "implementMaxCycles": {
      "description": "Omit to leave unchanged; null clears the session override and returns to repo/user config.",
      "format": "uint8",
      "minimum": 0.0,
      "type": [
        "integer",
        "null"
      ]
    },
    "implementMode": {
      "anyOf": [
        {
          "anyOf": [
            {
              "$ref": "#/definitions/ImplementMode"
            },
            {
              "type": "null"
            }
          ]
        },
        {
          "type": "null"
        }
      ],
      "description": "Omit to leave unchanged; null clears the session override and returns to repo/user config."
    },
    "issueTypes": {
      "description": "Omit to leave unchanged; null clears the session override and returns to repo/user config.",
      "items": {
        "$ref": "#/definitions/RepoCiIssueType"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "longCi": {
      "description": "Omit to leave unchanged; null clears the session override and returns to repo/user config.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "mode": {
      "anyOf": [
        {
          "anyOf": [
            {
              "$ref": "#/definitions/RepoCiSessionMode"
            },
            {
              "type": "null"
            }
          ]
        },
        {
          "type": "null"
        }
      ],
      "description": "Omit to leave unchanged; null clears the session override and returns to repo/user config."
    },
    "reviewRounds": {
      "description": "Omit to leave unchanged; null clears the session override and returns to repo/user config.",
      "format": "uint8",
      "minimum": 0.0,
      "type": [
        "integer",
        "null"
      ]
    },
    "threadId": {
      "type": "string"
    }
  },
  "required": [
    "threadId"
  ],
  "title": "ThreadRepoCiSessionConfigSetParams",
  "type": "object"
}